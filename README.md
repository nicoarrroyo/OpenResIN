# Open-source Reservoir Identifier and Navigator (OpenResIN) 

## Description
The Open-source Reservoir Identifier and Navigator (OpenResIN) is a Python-based project designed to automate the workflow of generating labelled training data from satellite imagery and using this data to train and deploy a machine learning model for water body identification.

The pipeline consists of several key components:
1.  **Navigable Automated Labelling Interface for Regions of Attention (NALIRA)** (`label.py`, run as `openresin-label`): The `label.py` file itself is the orchestrator for the operative file: `labelling.py`. The operative file processes Sentinel-2 satellite imagery, calculates the Normalized Difference Water Index (NDWI), performs cloud masking, and provides an interactive GUI for manually labelling regions of interest (reservoirs, water bodies, land, and sea). It generates the initial labelled coordinates and segments the image data into smaller classified images (mini-chunks) used for training.
2.  **Keras Reservoir Identification Sequential Platform (KRISP) - Trainer** (`train.py`, run as `openresin-train`; plus `modelling.py` and `epoch_pathfinder.py`): Uses the segmented image data generated by NALIRA to train a Keras Sequential convolutional neural network (CNN). It includes a helper script (`epoch_pathfinder.py`) to determine the optimal number of training epochs to prevent overfitting.

3.  **KRISP - Predictor (KRISPY)** (`predict.py`, run as `openresin-predict`; plus `inference.py`): Loads the trained model and deploys it to classify mini-chunks across an entire satellite image tile, generating a prediction file. 
4.  **KRISP External Technical Testing Environment (KRISPETTE)** (`evaluate.py`, run as `openresin-evaluate`): This component does not run from a clean clone as it stands. This is the evaluation script written for the initial undergraduate project. It is kept in the tree because it is the tool that was used to produce the figures in my dissertation, but it is no longer a tool that works with the rest of the pipeline, and is therefore being rewritten completely. 
5.  **Supporting Modules**: Includes scripts for data handling (`data_handling.py`), image processing (`image_handling.py`), user interfacing elements (`user_interfacing.py`), and miscellaneous utilities (`misc.py`).

The overall goal is to provide a streamlined process from raw satellite data to a trained model capable of identifying water features.

## Features
* **End-to-End Pipeline:** Covers data preparation, labelling, training, and prediction.
* **Sentinel-2 Focused:** Optimised for processing Sentinel-2 `.SAFE` format imagery.
* **Water Index Calculation:** Implements NDWI.
* **Cloud Masking:** Uses OmniCloudMask with Sentinel-2 bands to mask clouds.
* **Interactive Labelling GUI:** Tkinter-based interface (`openresin-label`) for drawing bounding boxes around features of interest.
* **Automated Data Segmentation:** `openresin-label` automatically crops and saves labelled image examples into class-specific folders.
* **CNN Model Training:** `openresin-train` trains a sequential CNN model using TensorFlow/Keras.
* **Epoch Optimisation:** `epoch_pathfinder.py` helps visualise training/validation curves to select an appropriate number of epochs.
* **Batch Prediction:** `openresin-predict` efficiently processes large images in batches for prediction.
* **Performance Evaluation**: `openresin-evaluate` provides a testing environment to evaluate the model's performance.
* **Modular Code:** Organised into distinct Python scripts for different functionalities.

## Installation
1.  **Clone the repository:**
    ```bash
    git clone https://github.com/nicoarrroyo/OpenResIN.git
    cd OpenResIN
    ```

2.  **Install Required Libraries:**
    This project requires Python (tested with 3.11) and several libraries. You can install them using pip:
    ```bash
    pip install numpy matplotlib Pillow tensorflow omnicloudmask geopandas fiona rasterio shapely
    ```
    * `numpy`: For numerical operations and array handling.
    * `matplotlib`: For plotting graphs.
    * `Pillow`: For image manipulation.
    * `tensorflow`: The core machine learning library.
    * `tkinter`: Usually included with Python standard library, used for the GUI in `label.py`. If you don't have it, install it with your machine's package manager.
    * `omnicloudmask`: For cloud masking.
    * `geopandas`: For working with geospatial data.
    * `fiona`: For reading and writing geographic data files.
    * `rasterio`: For reading and writing raster datasets.
    * `shapely`: For reading masking files.
    * `cupy`: (optional, encouraged only for CUDA-compatible GPUs) For speeding up GPU-bound tasks like image compositing. Install with `pip install -e ".[gpu]"`.

3.  **Install the package:**
    Installing puts the four pipeline stages on your PATH as console scripts, and lets the modules import each other from anywhere:
    ```bash
    pip install -e .
    ```

## Usage Instructions
To run the entire pipeline, follow these steps sequentially. Each step is a command that you can run, created when you ran `pip install -e .`. Each command takes `-h` or `--help` as input so you can see the possible flags and defaults. 

**Assumptions:**
* You have installed the package (see above).
* You have downloaded a Sentinel-2 satellite image tile in `.SAFE` format.
* You have downloaded the necessary masking files.

**1. Setup File Structure**
* **Extract Code and Image:** Extract the downloaded code and satellite image to the intended directory (in `data` folder).


**2. Training Data Generation (`openresin-label`)**
* Run `openresin-label`
* The script will process the satellite bands and calculate water indices.
* A Tkinter window will appear for data labelling.
* After labelling, the script will automatically segment the images and save them as PNGs in the appropriate class folders.

**3. Model Training (`openresin-train` & Epoch Pathfinder)**
* **Determine Optimal Epochs:** run `python -m openresin.epoch_pathfinder` and examine the plots to choose the optimal number of epochs.
* **Train the Final Model:**
    ```bash
    openresin-train --epochs 150 --save-model
    ```
    * `--save-model` is required to write the model to `models/`; the default is off, so a plain run trains without saving.
    * The model is saved as `{model type} model epochs-{epochs}.keras`, which is the name the predict stage looks for.
    * Exits non-zero if training does not complete.

**4. Model Deployment/Prediction (`openresin-predict`)**
* Run `openresin-predict`
* It loads the trained model, runs predictions, and appends the results to the tile's CSV, resuming from the last chunk already written.
* Once a tile is complete, the same command processes the results and draws the maps instead.
* `--model-epochs` and `--model-type` must match the model you trained; both default to the config, so they line up with `openresin-train` on a default run.

**5. Accuracy Assessment (`openresin-evaluate`)**
* Run `openresin-evaluate` (! under re-development)

## Repository Structure
This repository is organised into several key directories:

- `data/`: Inputs. Contains all the directories in which the user must put their raw data for the project, plus the seed labels that ship with the repository.
	- `seed-labels/`: Public, hand-labelled seed label data. Tracked by git, and read-only as far as the pipeline is concerned.
    	- `T31UCU-5000chunks.csv`: Labelled chunk coordinates for tile T31UCU, used as a starting point so a fresh clone can train without labelling first.
    - `masks/`: Directory for user to organise masking files.
        - `boundaries/`: Country boundary masks.
        - `known-reservoirs/`: Masks for known reservoirs.
        - `rivers/`: River masks.
        - `terrain/`: Terrain level / gradient masks.
        - `urban-areas/`: Urban area (cities, towns, etc.) masks.
    - `sat-images/`: Directory for user-downloaded Sentinel-2 satellite imagery.

- `outputs/`: Everything the pipeline generates. Not tracked by git; arrives empty on a fresh clone.
    - `labels/`: Label coordinates from your own labelling sessions, kept separate from the tracked seed set.
    - `patches/`: Segmented training images, one sub-folder per class.
    - `predictions/`: Model predictions across a whole tile.

- `models/`: Stores trained model versions (initially empty).

- `src/`: Houses the source code for the project.
    - `data_handling.py`: Helper scripts containing functions for loading, preprocessing, and managing data.
    - `epoch_pathfinder.py`: Helps determine the optimal number of training epochs for `train.py`.
    - `image_handling.py`: Helper script containing functions for image manipulation and processing.
    - `inference.py`: Large function file used for model deployment. Core prediction logic.
    - `predict.py`: `openresin-predict`. Runs predictions on full tiles and draws the maps.
    - `evaluate.py`: `openresin-evaluate`. Confusion matrix simulation script. Testing environment for `krisp` models.
    - `krisp_config.py`: Settings and path anchors for the train, predict and evaluate stages.
    - `modelling.py`: Dataset loading, model building, training and saving.
    - `train.py`: `openresin-train`. Model training script.
    - `misc.py`: A collection of miscellaneous utility functions.
    - `label.py`: `openresin-label`. Main script for data generation and labelling GUI.
    - `nalira_config.py`: Settings and path anchors for the label stage.
    - `labelling.py`: The eight labelling steps that `label.py` orchestrates.
    - `user_interfacing.py`: Helper script containing functions for running user interactions.

- `tests/`: Test suite. Run with `python -m pytest tests`.
    - `test_config.py`: Checks the config path anchors survive a changed working directory.
    - `test_imports.py`: Checks every module imports without side effects and that each stage exposes a `main()`.
    - More to come

## Known Limitations
- STM Compositing method going on CPU (using percentile calculation from `numpy`) doesn't work, at least on my CPU, for the whole image (it just crashes). The GPU percentile method works but that needs a beefy graphics card.
- `krisp` section of the pipeline is under active development but is currently non-functional.
- `LP_MODE` in NALIRA does not support data segmentation (this is unlikely to change any time soon)

## License
This project is licensed under the Apache 2.0 License - see the LICENSE file for details.

## Contact
* Nicolas Arroyo, nicolas.renato.arroyo@gmail.com, he/him
